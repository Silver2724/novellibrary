# Name of Application
spring.application.name=novellibrary

# H2 in-memory database setup
spring.datasource.url=jdbc:h2:mem:noveldb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate settings
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# H2 console (dev only)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# API Key(s)
GOOGLE_BOOKS_API_KEY=AIzaSyCPBPvQBNuSa9aLUSpQj4LSvhN_oS2-P4M

# Bind to all network interfaces
server.address=0.0.0.0

# Optional: explicitly set port
server.port=8080